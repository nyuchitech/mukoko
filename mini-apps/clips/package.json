{
  "name": "@mukoko/clips",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured'",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "preact": "^10.25.0",
    "@mukoko/ui": "workspace:*",
    "@mukoko/bridge": "workspace:*",
    "@mukoko/api": "workspace:*",
    "@mukoko/types": "workspace:*"
  },
  "devDependencies": {
    "@mukoko/tsconfig": "workspace:*",
    "@preact/preset-vite": "^2.9.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
